# Sales & Returns Data Analysis (Python + Pandas)

Loads sample `orders.csv` and `rma.csv` to compute KPIs:
- Top products sold
- Returns by state
- Return rate by product

## Run
```bash
python analyze.py
```